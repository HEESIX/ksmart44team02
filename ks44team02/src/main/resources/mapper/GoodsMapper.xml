<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ks44team02.mapper.GoodsMapper">
	<!-- 상품 카테고리 리스트(사용중지된 카테고리까지)  -->
	<select id="getGoodsCategoryList" resultType="GoodsCategory">
		SELECT
			g_cate_code 	as goodsCategoryCode
			,cate_name 		as goodsCategoryName
			,cate_reg_date 	as goodsCategoryRegDate
			,member_id 		as goodsCategoryRegId
			,cate_status 	as goodsCategoryStatus
		FROM
			tb_goods_cate;
	</select>
	<!-- 상품 카테고리 리스트(사용중지된 카테고리 제외)  -->	
	<select id="getGoodsCategoryListBuyer" resultType="GoodsCategory">
		SELECT
			g_cate_code 	as goodsCategoryCode
			,cate_name 		as goodsCategoryName
		FROM
			tb_goods_cate
		WHERE
			cate_status = 'Y';
	</select>
	<!-- 특정 상품 카테고리 정보 조회(상품 카테고리 수정화면) -->
	<select id="getGoodsCategoryInfo" parameterType="String" resultType="GoodsCategory">
		SELECT
			gc.g_cate_code		AS goodsCategoryCode
			,gc.cate_name		AS goodsCategoryName
			,gc.cate_reg_date	AS goodsCategoryRegDate
			,gc.member_id		AS goodsCategoryRegId
			,gc.cate_status		AS goodsCategoryStatus
		FROM
			tb_goods_cate AS gc
		WHERE
			gc.g_cate_code = #{goodsCategoryCode};
	</select>
	<!-- 상품 카테고리 수정 처리  -->
	<update id="modifyGoodsCategory" parameterType="GoodsCategory">
		UPDATE tb_goods_cate AS gc
		SET 
			gc.cate_name = #{goodsCategoryName}
			,gc.cate_status = ${goodsCategoryStatus}
		WHERE 
			gc.g_cate_code = #{goodsCategoryCode};
	</update>
</mapper>